<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9Oer-2uLRyqOQ4zWJgoraK7aCD/L+jXWdBCX1w56tW+xedC" crossorigin="anonymous">
    <style>
        /* Estilos personalizados para el layout general y para que el contenedor resalte */
        body {
            padding: 20px;
            background-color: #f8f9fa; /* Fondo gris claro */
            font-family: Arial, sans-serif; /* Fuente más común */
        }
        .container {
            max-width: 600px; /* Limita el ancho del contenido principal */
            margin: 50px auto; /* Centra el contenedor y le da margen superior e inferior */
            background-color: #ffffff; /* Fondo blanco para el contenedor */
            border-radius: 10px; /* Bordes más redondeados */
            box-shadow: 0 6px 12px rgba(0,0,0,0.15); /* Sombra más pronunciada */
            padding: 40px; /* Más relleno interno */
        }
        /* Ajuste para el h1 */
        h1 {
            color: #343a40; /* Color de texto más oscuro */
            font-weight: 700; /* Negrita más marcada */
            margin-bottom: 1.5rem; /* Margen inferior */
        }
        /* Estilos para el texto de saludo y el botón de salir */
        .saludo-linea {
            display: flex; /* Habilita Flexbox */
            align-items: center; /* Centra verticalmente */
            /* justify-content-between; -- ELIMINAMOS ESTO para que no se separe al máximo */
            margin-bottom: 1.5rem; /* Margen inferior */
            font-size: 1.3rem; /* Tamaño de fuente un poco más grande */
            color: #495057; /* Color de texto sutil */
        }
        .saludo-nombre {
            font-weight: bold; /* Nombre en negrita */
            color: #007bff; /* Color azul de Bootstrap para el nombre */
        }
        /* Estilos para los párrafos de edad */
        p {
            line-height: 1.6; /* Interlineado para mejor lectura */
        }
        .lead {
            color: #6c757d; /* Color para el mensaje de edad */
        }
        /* Estilo para los campos de formulario */
        .form-control {
            border-radius: 5px;
            border: 1px solid #ced4da;
            padding: 0.75rem 1rem;
        }
        .form-control:focus {
            border-color: #80bdff;
            box-shadow: 0 0 0 0.2rem rgba(0,123,255,.25);
        }
        /* Estilo para los botones */
        .btn {
            border-radius: 5px;
            padding: 0.75rem 1.25rem;
            font-weight: bold;
            text-transform: uppercase; /* Texto en mayúsculas */
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .btn-success {
            background-color: #28a745;
            border-color: #28a745;
        }
        .btn-success:hover {
            background-color: #218838;
            border-color: #1e7e34;
        }
        /* Clase para el botón Salir - ¡Más llamativo! */
        .btn-salir-personalizado {
            background-color: #dc3545; /* Rojo fuerte de Bootstrap (danger) */
            border-color: #dc3545;
            color: white; /* Texto blanco */
            transition: background-color 0.3s ease; /* Transición suave al pasar el ratón */
            min-width: 90px; /* Ancho mínimo reducido */
            text-align: center; /* Centra el texto en el botón */
            padding: 0.5rem 1rem; /* Relleno más pequeño para hacerlo más compacto */
            font-size: 0.9rem; /* Fuente un poco más pequeña */
        }
        .btn-salir-personalizado:hover {
            background-color: #c82333; /* Rojo más oscuro al pasar el ratón */
            border-color: #bd2130;
            color: white;
        }

        /* ¡NUEVA CLASE PARA EL MARGEN ESPECÍFICO DE 2CM! */
        .margin-left-2cm {
            margin-left: 2cm; /* O 20mm, o 75.6px si prefieres píxeles */
        }

    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center">{{title}}</h1>

        {{#if conocido}}
            <p class="saludo-linea">
                <span>Hola <span class="saludo-nombre">{{nombre}}</span></span>
                <a href="/salir" class="btn btn-salir-personalizado margin-left-2cm">SALIR</a> 
            </p>
            
            {{!-- Mostrar la edad y el mensaje de edad --}}
            {{!-- {{#if edad}}
                <p class="mb-1">Tu edad es: <span class="fw-bold">{{edad}}</span></p>
                {{#if mensajeEdad}}
                    <p class="lead text-muted">{{mensajeEdad}}</p>
                {{/if}}
            {{/if}} --}}
        {{/if}}

        {{#unless conocido}}
            <p class="mt-4">Usuario:</p>
            <form action="/Ingresar" method="post">
                <input type="text" name="nombre" placeholder="Tu nombre de usuario" class="form-control mb-3">
                <p>Contraseña:</p>
                <input type="number" name="edad" placeholder="Tu contraseña" min="1" class="form-control mb-3">
                <button type="submit" class="btn btn-primary">Ingresar</button>
            </form>
            {{#if error}}
            <p>usuario y contraseña incorrecto</p>
            {{/if}}
        {{/unless}}
    </div>

    </body>
</html>