<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9Oer-2uLRyqOQ4zWJgoraK7aCD/L+jXWdBCX1w56tW+xedC" crossorigin="anonymous">
    
</head>
<body>
    <div class="container">
        <h1 class="text-center">{{title}}</h1>

        {{#if conocido}}
            <p class="saludo-linea">
                <span>Hola <span class="saludo-nombre">{{nombre}}</span></span>
                <a href="/salir" class="btn btn-salir-personalizado margin-left-2cm">SALIR</a> 
            </p>
            
            {{!-- Mostrar la edad y el mensaje de edad --}}
            {{!-- {{#if edad}}
                <p class="mb-1">Tu edad es: <span class="fw-bold">{{edad}}</span></p>
                {{#if mensajeEdad}}
                    <p class="lead text-muted">{{mensajeEdad}}</p>
                {{/if}}
            {{/if}} --}}
        {{/if}}

        {{#unless conocido}}
            <p class="mt-4">Usuario:</p>
            <form action="/admin/login" method="post">
                <input type="text" name="usuario" placeholder="Tu nombre de usuario" class="form-control id="usuario" mb-3">
                <p>Password:</p>
                <input type="password" name="password" placeholder="Tu contraseña" min="1" class="form-control id="password" mb-3">
                <button type="submit" class="btn btn-primary">Enviar</button>
            </form>
            {{#if error}}
            <p>usuario y contraseña incorrecto</p>
            {{/if}}
        {{/unless}}
    </div>





<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script> 
    </body>
</html>