<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Contenido</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/stylesheets/styleABMContenido.css" rel="stylesheet">
</head>

<body>
    <div class="container-fluid">
        {{!-- Aquí puedes poner tu barra de navegación o encabezado --}}

        <div class="row">
            <div class="col-6 offset-3">
                <div class="card p-4 mx-auto form-red-bg" style="max-width: 600px;">
                    <h2 class="text-center resaltado-amarillo">Agregar Nueva Foto</h2>

                    <form action="/admin/ABMContenido/agregar" method="post" enctype="multipart/form-data">
                        
                        <div class="mb-3">
                            <label for="evento" class="form-label">Evento</label>
                            <input type="text" class="form-control" id="evento" name="evento" required>
                        </div>
                        <div class="mb-3">
                            <label for="categorias" class="form-label">Categorías</label>
                            <input type="text" class="form-control" id="categorias" name="categorias" required>
                        </div>
                        <div class="mb-3">
                            <label for="descripcion" class="form-label">Descripcion</label>
                            <input type="text" class="form-control" id="descripcion" name="descripcion" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Subir Archivo (Fotos o Videos)</label>
                            <div class="input-group">
                                <label class="btn btn-rojo-amarillo">Examinar...
                                    <input type="file" class="form-control d-none" id="imagen-upload" name="imagen" accept="image/*,video/*" required>
                                </label>
                                <span class="form-control" id="file-name" style="background-color: white;">No se seleccionó un archivo.</span>
                            </div>
                        </div>
                        
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-murialdo">Guardar</button>
                        </div>
                    </form>
                    
                    {{#if error}}
                        <p class="text-warning mt-2">{{message}}</p>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        document.getElementById('imagen-upload').addEventListener('change', function() {
            var fileName = this.files.length > 0 ? this.files[0].name : 'No se seleccionó un archivo.';
            document.getElementById('file-name').textContent = fileName;
        });
    </script>
</body>
</html>